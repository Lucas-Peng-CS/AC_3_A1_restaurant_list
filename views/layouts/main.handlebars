<header>
  <!-- navigation -->
  <nav class="navbar navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">
        <img
          src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png"
          width="30"
          height="30"
          class="d-inline-block align-top"
          alt="我的餐廳清單"
        />
        {{#if user.name}}{{user.name}}的{{/if}}餐廳清單
      </a>
      <div class="btn">
        <a
          class="btn btn-primary mr-2"
          href="/restaurants/new"
          role="button"
        >Create
        </a>

        {{#if isAuthenticated}}
          <a href="/users/logout" class="btn btn-outline-danger">Logout</a>
        {{else}}
          <a href="/users/login" class="btn btn-outline-danger">Login</a>
        {{/if}}
      </div>
   
    </div>
  </nav>

  <div class="banner"></div>
  <link rel="stylesheet" href="/stylesheets/bootstrap.css" />
  <script src="https://kit.fontawesome.com/9dc230e89d.js"></script>
  <link rel="stylesheet" href="/stylesheets/main.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
</header>

<body>
  <!-- parital templates will replace the part of "body" here -->
  {{{body}}}

  <script src="/javascripts/jquery-3.3.1.js"></script>
  <script src="/javascripts/popper.js"></script>
  <script src="/javascripts/bootstrap.js"></script>
</body>